# {{WORKFLOW_TITLE}}

## 调查目标

在这里写这个工作流要解决的问题、背景和边界。

## 工作分解

在这里写是否需要并发、需要多少条独立调查方向，以及每条方向的职责。

## 工具调用契约

在这里逐项写明：

- 必须调用的 MCP server 和具体 tool。
- 哪条调查方向负责调用。
- 每个工具的调用次数、输入要求和证据要求。
- 工具不可用、返回失败或证据不足时如何 fail closed。

不要把本工作流的 MCP 选择或调用顺序移动到通用 agent、skill 或 Python CLI。

## 输出要求

在这里写最终交付格式、来源要求、质量标准和停止条件。
